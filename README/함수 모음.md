```java
upper : 소문자를 대문자로 만드는 함수

lower : 대문자를 소문자로 변환해주는 함수
```

```java
strip : 문자열의 양쪽 공백을 모두 지워라 함수

```java
stripLeading() : 왼쪽 공백 제거

stripTrailing() : 오른쪽 공백 제거
```

```java
replace : 특정 문자를 치환해주는 기능이다. (  치환 : 바꾼다 )

String str = "apple";
System.out.println(str.replace('p', 'b'));

출력 : abble
```
```java
어떤 값을 없애버리고 싶을 경우 

String str = "apple";
str = str.replace("p", "")

System.out.println(str);

출력 : ale
```
# 문자열 확인(조회) 함수


```java
1. length() : 길이 보기
String str = "hello";
System.out.println(str.length());

 결과: 5
```









